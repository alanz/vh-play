-- Startup commands for the GHC interpreter
-- :set -hide-package monads-tf
-- :set -hide-package monads-fd
:set -i./src/
-- :set -i./dist/build
-- :set -v
:set -package ghc
-- (setq ghc-ghc-options '("-i./src"))
